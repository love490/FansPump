{
  "name": "@iopn/contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test -vv",
    "coverage": "forge coverage",
    "deploy:local": "forge script script/Deploy.s.sol --rpc-url http://127.0.0.1:8545 --broadcast",
    "deploy:iopn": "forge script script/Deploy.s.sol --rpc-url $IOPN_RPC_URL --broadcast --verify"
  }
}
