# Copy to packages/database/.env and paste your Railway PUBLIC Postgres URL.
# Railway → Postgres → Connect → Public Network (NOT postgres.railway.internal)
DATABASE_URL="postgresql://postgres:PASSWORD@HOST.proxy.rlwy.net:PORT/railway"
