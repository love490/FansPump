import { HomeDashboard } from "@/components/home/home-dashboard";

/** In-app home — token dashboard experience (formerly at /). */
export default function AppHomePage() {
  return <HomeDashboard />;
}
