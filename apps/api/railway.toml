[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --no-frozen-lockfile && pnpm db:generate && pnpm --filter @iopn/api build"

[deploy]
startCommand = "pnpm --filter @iopn/api start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
